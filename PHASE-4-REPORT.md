# Phase 4 实施说明：DeepSeek 双语 AI Daily

## 已完成

- 增加稳定、固定的系统提示词，并始终把它作为 DeepSeek 请求的第一条消息。
- 一次批量提交已验证、清洗后的候选正文；输入有文章数、单篇字符数和最大输出 token 的限制。
- 对模型 JSON 进行严格校验：条目数和候选 ID 必须一一对应；原文要点和翻译必须成对；英文文章必须有 2–4 条实用表达。
- 所有来源、发布时间和原文链接由本地 `articles` 表回填，防止模型编造或替换可追溯元数据。
- 将双语内容保存在 `article_enrichments` 表，将 DeepSeek 响应的实际 token/cache 用量保存在 `model_usage` 表。
- 同一候选完成整理后不会再次进入默认批次。

## 本阶段不包含

- 飞书资讯中心页面、归档页或首页接入。
- 自动定时、推送或对话入口。
- 费用估算或仪表盘展示。

## 验收重点

执行 `py -3 run_enrichment.py` 后，检查 `data/latest-enrichment.json`：每条都应含有原文链接、来源、发布时间、原文要点、逐点中文翻译、中英文摘要、相关性和实用表达；顶层 `usage` 应保留 API 实际返回的 token/cache 字段。
